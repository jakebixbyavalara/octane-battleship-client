import board from 'octane-battleship-client/utils/board';
import { module, test } from 'qunit';

module('Unit | Utility | board', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = board();
    assert.ok(result);
  });
});
